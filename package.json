{
  "name": "market-pulse",
  "private": true,
  "workspaces": [
    "packages/*",
    "apps/*",
    "server/*"
  ],
  "scripts": {
    "dev": "npm run dev:server & npm run dev:apps",
    "dev:server": "npm run dev --workspace=server/market-simulator",
    "dev:apps": "npm run dev --workspace=apps/shell & npm run dev --workspace=apps/market-overview & npm run dev --workspace=apps/watchlist & npm run dev --workspace=apps/asset-details & npm run dev --workspace=apps/trade-stream & npm run dev --workspace=apps/alerts",
    "build": "npm run build --workspaces",
    "build:packages": "npm run build --workspace=packages/contracts && npm run build --workspace=packages/utils && npm run build --workspace=packages/state && npm run build --workspace=packages/market-client && npm run build --workspace=packages/ui",
    "clean": "rm -rf node_modules packages/*/dist apps/*/dist server/*/dist"
  },
  "devDependencies": {
    "typescript": "^5.4.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
